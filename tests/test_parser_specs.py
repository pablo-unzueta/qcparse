import pytest

@pytest.fixture
def parser_specs():
    # Setup code to generate parser_specs
    return parser_specs  # Return the generated parser_specs

def test_parser_specs(parser_specs):
    print("Parser Specs:", parser_specs)  # Print or assert as needed
